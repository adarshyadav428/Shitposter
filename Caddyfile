{$DOMAIN:localhost} {
    encode gzip zstd
    reverse_proxy app:8000

    @feed path /rss /feed /feed.xml /events.json
    handle @feed {
        reverse_proxy app:8000
    }

    log {
        output stdout
        format console
    }
}
